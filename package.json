{
  "homepage": "https://zidankusumafirdaus.github.io/Study-Tailwind/",
  "name": "tailwind-css",
  "version": "1.0.0",
  "main": "tailwind.config.js",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d public",
    "build": "npx tailwindcss -i ./src/input.css -o ./public/style.css",
    "dev": "npx tailwindcss -i ./src/input.css -o ./public/style.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
